# Credentials — NEVER commit
_credentials/
.credentials/
secrets/
*.key
*.pem
*.secret
.env
.env.*

# System
.DS_Store
Thumbs.db

# Obsidian workspace state
.obsidian/workspace.json
.obsidian/workspace-mobile.json

# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key

# end-session lock files
.claude/.end-session-*

# Manuscript build output — disposable, regenerated by scripts/build-manuscript.py.
# _snapshots/ is the permanent record and stays tracked.
03-Theology/Eschatology-Framework/manuscript/_build/

# MemPalace per-project files (issue #185)
mempalace.yaml
entities.json

# Python bytecode from scripts/test-build-checks.py importing the build module
__pycache__/
*.pyc
